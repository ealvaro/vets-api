Schemas are maintained in vets-json-schema and are additionally utilized by FormsAPI

At this time the schema file name is also appended with '-V2'

In the future the schema may be stored within the module

@see modules/dependents_benefits/lib/dependents_benefits/claim_behavior/form_validation.rb
